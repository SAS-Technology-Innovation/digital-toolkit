# Vercel Environment Variables
# Copy this file to .env.local and fill in your values

# ============================================
# Apps Script Settings (Required)
# ============================================
APPS_SCRIPT_URL=https://script.google.com/macros/s/YOUR_SCRIPT_ID/exec
FRONTEND_KEY=your-frontend-key-here

# ============================================
# Edge Config - Status Page (Required for /status)
# ============================================
# Edge Config connection string (auto-set when Edge Config is created in Vercel dashboard)
EDGE_CONFIG=https://edge-config.vercel.com/YOUR_EDGE_CONFIG_ID?token=YOUR_TOKEN

# Edge Config ID for API updates (found in Vercel dashboard)
EDGE_CONFIG_ID=ecfg_your_edge_config_id

# Vercel API token for Edge Config updates (create at vercel.com/account/tokens)
VERCEL_TOKEN=your_vercel_api_token

# ============================================
# Cron Job Protection
# ============================================
# Secret for authenticating cron job requests
# Generate with: openssl rand -base64 32
CRON_SECRET=generate_with_openssl_rand_-base64_32

# ============================================
# Optional Settings
# ============================================
# Password for renewal page access
RENEWAL_PASSWORD=your-renewal-password
